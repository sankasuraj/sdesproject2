dependencies:
  pre:
    - pip install --upgrade pip
    - pip install -r requirements.txt
    
test:
  post:
    - bash <(curl -s https://codecov.io/bash)
    
COVERALLS_REPO_TOKEN=wPEpW9rwAWANrtcnNUKN0cSmzHSmfWtKg
